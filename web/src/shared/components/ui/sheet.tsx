// shadcn's Sheet is a directional Dialog. Keep this thin so the branded shell
// can retain its existing mobile navigation layout.
export {
  Dialog as Sheet,
  DialogClose as SheetClose,
  DialogContent as SheetContent,
  DialogOverlay as SheetOverlay,
  DialogPortal as SheetPortal,
  DialogTitle as SheetTitle,
} from './dialog';
