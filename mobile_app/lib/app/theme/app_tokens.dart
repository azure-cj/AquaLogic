/// Small layout tokens shared by the mobile page shell and common surfaces.
///
/// These are intentionally limited to values that have a clear product-wide
/// meaning. Feature-specific spacing should remain local when it expresses a
/// different hierarchy.
class AppSpacing {
  static const pageGutter = 16.0;
  static const pageTop = 14.0;
  static const sectionGap = 12.0;
  static const pageBottom = 24.0;
  static const bottomDockClearance = 76.0;
}

class AppRadii {
  static const standardSurface = 18.0;
  static const pageHeader = 26.0;
  static const pill = 99.0;
}
